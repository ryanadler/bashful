" Should go into the /etc/vimrc file for system wide access
" it provides defaults for vim to act in dark mode, ignore the case as you search, hilight as you type, and
" set the status of the file/location you are in while using vim
set laststatus=2
set statusline+=%F
set hlsearch
set ignorecase
set background=dark
